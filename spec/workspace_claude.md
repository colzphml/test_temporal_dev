# Изолированный воркспейс эксперимента ({{VARIANT}})

Правила этой папки ПЕРЕКРЫВАЮТ любые общие инструкции из родительских каталогов:

- Не используй вики-память, таск-трекеры, MCP-серверы и другие внешние инструменты.
- Не читай и не изменяй файлы за пределами этой папки (`../`, исходники моков и чекера).
- Не устанавливай пакеты — всё нужное уже стоит в `./.venv`.
- Задание — в `TASK.md`. Критерий готовности — успешный `make verify`.
